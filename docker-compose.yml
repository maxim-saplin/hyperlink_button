services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: hyperlink_button:dev
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "8501:8501"
    environment:
      UV_CACHE_DIR: /app/.uv-cache
    command: ["bash"]
